enum RequestState { idle, loading, success, error }

enum AuthState { signIn, signUp, otp, verify }

enum CartState { empty, hasItems }

enum ConnectionState { connected, disconnected }

enum ProductSortType { newest, priceLowToHigh, priceHighToLow, mostPopular }

enum AiFixingState { idle, analyzing, done, error }

enum ChatbotState { idle, typing, done, error }
